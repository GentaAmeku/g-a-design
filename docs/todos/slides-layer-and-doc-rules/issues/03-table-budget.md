# 03 — 表が折り返すかを、組む前に数えられるようにする

**What to build:** 表を組む前に、折り返すかを字数で判定できる。「3列まで」では4列を許す条件が決まらない。制約は列数ではなく、列ごとの最長セルの合計。

**Blocked by:** None — can start immediately.

**Status:** done

- [x] 規則が「Σ(列ごとの最長セル) ≤ 41.4 − 0.9 × 列数 字幅」になっている
- [x] 3列 38.7 / 4列 37.8 / 5列 36.9 の内訳と測り方が書いてある
- [x] 列数の上限は読みやすさ側の規則として4列で残る
- [x] check.sh が予算の式を tokens.css と document.css から計算し直す

`検証:` `./check.sh`
