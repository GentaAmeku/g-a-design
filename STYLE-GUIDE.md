# G.A Design — STYLE GUIDE

GentaAmeku の個人デザインシステム。**値の正本は [tokens.css](tokens.css)、根拠の正本がこの文書。**成果物は tokens.css を読み、この文書は「なぜその値なのか」と「どう測ったのか」を持つ。

目標読了時間 6分。

## 何を決めているか

決めているのは三つ。**地と墨の段**、**書体と大きさ**、**余白**。この三つが揃っていれば、1枚の HTML でもスライドでも同じ意匠で出る。部品(`.gad-*`)はその三つを組み合わせただけで、独自の値を持たない。

目的は判断の回数を減らすこと。色を選ぶ、大きさを選ぶ、影を付けるかを成果物ごとに決めていると、意匠がずれていく。ずれを止めるために、選べる幅のほうを削ってある。

## 色 — 墨4段と、有彩色2つ

地は純白 `#ffffff`。要約とコードの沈んだ地だけ `#f6f6f6` に落とす。

墨は濃い順に4段。明暗差(コントラスト比)はすべて実測値で、白地と沈んだ地の両方に対して測ってある。

| token | 値 | 白地 | 沈んだ地 | 使いどころ |
|---|---|---|---|---|
| `--gad-ink` | `#0a0a0a` | 19.80 | 18.32 | 本文と見出し |
| `--gad-ink-muted` | `#5c5c5c` | 6.69 | 6.19 | 脇の文、表の見出し、理由の行 |
| `--gad-ink-quiet` | `#6e6e6e` | 5.10 | 4.72 | ラベル、キャプション。**文字はここまで** |
| `--gad-mark` | `#8a8a8a` | 3.45 | 3.19 | 意味を持つ線と印。文字には使わない |

`--gad-ink-quiet` が文字の下限なのは、沈んだ地の上で 4.72 まで落ちるから。WCAG の本文しきい値は 4.5 なので、ここから一段下げると沈んだ地で割る。`--gad-mark` の 3.45 は線と印のしきい値 3.0 を超えるが、本文の 4.5 には届かない。だから引用の縦線や箇条の番号には使えて、文には使えない。

**薄く見せたいときは段を下げる。`opacity` は使わない。**透過は下の地によって実際の明暗差が変わるので、一度測った値が当てにならなくなる。

有彩色は2つだけ。

- `--gad-sign` `#f7e017`(署名の黄)— 白地に 1.34。読ませる用途には使えない。題名の下に 72×5px を1本引くだけの飾りとして持つ
- `--gad-alert` `#e05a81`(危険の rose)— 地として 3.53、その上に載る `--gad-ink` で 5.61。塗りつぶして使い、言葉を必ず添える

**状態を色だけで伝えない。**印は形を変え、言葉を並べる。白黒で刷ると黄は消えるので、そこで identity を担うのは署名行の文字になる。

暗い配色は持たない。`color-scheme: light` を固定し、端末が暗い設定でもこの意匠のまま出す。影も持たない。奥行きは地の明るさの差と、1px の髪の毛線(`--gad-hairline` `#e6e6e6`)で出す。

## 書体 — 本文 Zen Kaku Gothic New / 見出し BIZ UDPMincho 700

Google Fonts で日本語を持つ family は 68。Display・手書き・単一ウェイトを除くと、長文の本文に使えるのは7つ、明朝の見出しは4つに絞られる。この母集団を Chrome の `canvas.measureText` で測った(`font-size: 100px` を指定したときのインクの高さ)。

**同じ `font-size` でも実効サイズが 7% 違う。**漢字のインク高は 83.6 から 89.9 まで開いた。

| 役割 | 書体 | 漢字インク高 | 実装ウェイト |
|---|---|---|---|
| 本文 | Zen Kaku Gothic New | 83.6(母集団の最小) | 300 / 400 / 500 / 700 / 900 |
| 見出し | BIZ UDPMincho | 89.9(明朝の最大) | 400 / 700 のみ |

測定だけを見れば BIZ UDPGothic(89.7)と Zen Old Mincho の組が最良だった。見本を並べて比べた結果、2026-08-29 に本人が上の組を選んでいる。**本文の書体が母集団で最小なので、本文サイズを 17px から 18px へ上げて釣り合いを取ってある。**「大きめの文字」が欲しいときは、サイズを上げるより先に書体を替えるほうが効く。

**BIZ UDPMincho に 600 は無い。**400 と 700 だけなので、`font-weight: 600` はブラウザ側で 700 に解決される。CSS には 700 と書く。中間の段が欲しいときは、本文側の Zen Kaku Gothic New が持つ 500 を使う。

出典は Google Fonts の family メタデータ(<https://fonts.google.com/metadata/fonts>)と、Chrome 上の `canvas.measureText` による自測(2026-08-29)。母集団の全一覧、描画幅、選ばなかった書体の評価は [research/typeface.md](research/typeface.md) にある。

## 大きさと余白

| 役割 | token | 値 |
|---|---|---|
| 題名 | `--gad-size-title` | 34px |
| 見出し | `--gad-size-h2` | 22px |
| 小見出し | `--gad-size-h3` | 17px |
| 本文 | `--gad-size-body` | 18px |
| 表・コード | `--gad-size-small` | 15px |
| 注記・脇 | `--gad-size-fine` | 14px |
| ラベル | `--gad-size-label` | 12px |

行送り 1.95、字間 0.03em(見出し 0.05em、ラベル 0.16em)。行長は全角34〜36字で、本文幅 `--gad-measure: 640px` がこれにあたる。これ以上伸ばすと、行末から次の行頭へ目が戻れなくなる。

余白は3段。節と節が 44px、塊と塊が 22px、行と行が 14px。紙面の内側は `64px 72px 72px`、箱の内側は `22px 26px`、角丸は 6px。**文字を敷き詰めない。**

紙に出すときは `@page` を `18mm 16mm`、本文を 11pt にする。脇に置いた用語表は本文の後ろへ回し、見出しの直後で改ページしない。

## 部品 — 12種

実物は [component-samples.html](component-samples.html) にある。**組む前にブラウザで開く。**定義は [document.css](document.css)。

| 部品 | class | 使いどころ |
|---|---|---|
| 署名行 | `.gad-signature` / `.gad-foot` | 誰がいつ作ったか。上端と下端 |
| 要約 | `.gad-summary` | 冒頭に結論を置く |
| 目次 | `.gad-toc` | h2 が3つ以上あるとき |
| 用語表 | `.gad-glossary` | 本文で使う前に定義する |
| 引用 | `.gad-quote` | 原文と訳文は同じ大きさで並べる |
| コード | `pre.gad-code` | 色は付けない |
| 表 | `.gad-table` | 短い対応関係。3列まで |
| 図の枠 | `.gad-figure` | 枠とキャプション。中身は下の3つで描く |
| 決定 | `ol.gad-decided` | 1項目に1行の理由を添える |
| 未決 | `.gad-open` | 空でも節を残す |
| 注記 | `.gad-note` / `.gad-alert` | 補足は無彩色、注意だけ塗る |
| 並置 | `.gad-columns` | 2つか3つ。4つ以上は表 |

紙面の枠(署名行のラベル、目次の見出し)は英語でよい。中身の文は日本語のまま出す。

図は墨だけのインライン SVG で描く。塗り分けをせず、位置と形で意味を出す。ノードが増えるときは縦に伸ばし、矢印を交差させない。絵文字と矢印文字を図記号の代わりにしない。**流れの図は手で描かず、[figure/](figure/) の生成器に出させる**(下の「図の記法」)。

**SVG に色を直接書かない。**`stroke="#0a0a0a"` のような presentation attribute を使うと、そこだけ token の外に出る。`.gad-figure-frame` の中では次の3つを使う。

| class | 何を描くか | 引く色 |
|---|---|---|
| `.gad-node` | 箱、丸、意味を持つ線 | `--gad-ink` |
| `.gad-link` | 補助線、つなぎの線 | `--gad-mark` |
| `.gad-arrow` | 矢印の頭のような塗る印 | `--gad-mark` |

`<text>` は指定しなくても `--gad-ink` と本文の書体になる。

図が意味を持つなら `aria-hidden` を付けず、`figcaption` にその関係を文で書く。付けてよいのは、本文を読めば分かることの飾りだけ。

## 図の色 — 系列5色

**文書は読み込まない。**この5色は `tokens-figure.css` にあり、図と図表だけが読む。本文の有彩色は署名の黄と危険の rose の2つだけ、という規則を構造で守るため、ファイルごと分けてある。`document.css` からは参照しない。

| token | 色 | hex | 白地 | 沈んだ地 | 上の墨 |
|---|---|---|---|---|---|
| `--gad-series-1` | テラコッタ | `#b96c49` | 3.96 | 3.66 | 5.00 |
| `--gad-series-2` | オリーブ | `#878930` | 3.72 | 3.44 | 5.33 |
| `--gad-series-3` | ティール | `#18987f` | 3.60 | 3.33 | 5.50 |
| `--gad-series-4` | ブルー | `#4389c1` | 3.76 | 3.48 | 5.27 |
| `--gad-series-5` | パープル | `#9770b4` | 3.97 | 3.67 | 4.99 |

明度 61%、彩度 0.11 を全色で固定し、色相だけを 45 / 110 / 175 / 245 / 310 度に置いた。**明度を揃えたので、どれか1色だけが強く見えることがない。**明度を 65% にすると沈んだ地で 3.0 を割り、彩度を 0.12 以上にすると緑が sRGB の外へ出て丸められ、揃えた明度が崩れる。

危険の rose(彩度 0.17)と署名の黄とは、彩度と明度で離れている。系列の色が危険や署名と読み違えられることはない。

### 色は二重の手がかりに留める

**5色どうしの明暗差は 1.00〜1.10 しかない。**明度を揃えた代償で、白黒で印刷すると5色は同じ灰色に潰れる。設計書は PDF で配るので、これは実際に起きる。

だから**系列は色だけで区別しない。**形かラベルを必ず添え、色は同じ意味を重ねて示す二重の手がかりに留める。色だけの凡例は作らない。種別を伝えるのは形の仕事で、色が担うのは形では解けないこと — 重なる図形の見分け、隣接する領域の塗り分け、経路を目で追うこと — だけにする。

明度を振れば白黒でも分かれるが、そうすると濃い色が重要に、薄い色が副次的に見える。**系列に意味のない序列が生まれるほうが害が大きい。**

## 図の記法 — 流れの図

**流れの図は手で描かない。**小さな JSON を [figure/](figure/) の生成器に渡し、出てきた SVG をそのまま貼る。座標も色も入力に書かない。

描けるのは3つの形だけ。**縦の連鎖**(A→B→C)、**枝分かれ**(1つの親から横に並ぶ子)、**合流**(分かれた先が1点へ戻る)。この3つでフローチャートになる。

**戻る線は書けない。**入力が木の形なので、輪を作る構造そのものが無い。交差を許すと、線をよける計算と、それを確かめる検査が要る。参考にした archify はそれを持っていて、経路の計算だけで 57KB、共有の版付けと検査を合わせると 794KB ある(`renderers/shared/`、2026-08-31 に実測)。ここでは持たない代わりに、戻る線の要る図は「図にしない」か archify を使う。

### 種別は形で出す

色は使わない。系列5色は白黒で潰れるので、種別のような「どれか1つ」を伝える役には向かない(上の「色は二重の手がかりに留める」)。**この生成器は墨と印だけで描く。**

| kind | 呼び名 | 形 | ラベル上限 |
|---|---|---|---|
| `step` | 段 | 角丸長方形 | 10 字幅 |
| `branch` | 分かれ目 | 六角形 | 8 字幅 |
| `outside` | 外 | 破線の枠 | 10 字幅 |
| `store` | 保管 | 円筒 | 10 字幅 |

始点と終点は種別に持たせない。縦に流すので、一番上が入口、葉が出口だと位置で分かる。**語彙を増やすほど「どれを使うか」の判断が増える**ので、足りなくなってから足す。

2026-08-31 に3案を組んで並べ、本人が上の案(輪郭で分ける)を選んだ。比べたのは「輪郭を変える」「枠線の本数を変える」「同じ箱に印を添える」の3つで、62% に縮めたときに差が残るかどうかが分かれ目になった。並べた見本と、選ばなかった2案の評価は [research/figure-notation.md](research/figure-notation.md) にある。

### 寸法

| 何 | 値 |
|---|---|
| 箱 | 176 × 48px |
| 縦の間隔 | 44px |
| 横の間隔 | 20px |
| 箱の内側の余白 | 左右 12px |
| 六角形の尖り | 14px |
| 円筒の楕円 | 縦の半径 6px |
| 矢印の頭 | 8px |
| ノードの上限 | 9 個 |
| 横に並ぶ枝 | 3 つまで |

箱の幅 176px は、3つ横に並べた 568px が `.gad-figure-frame` の内寸 590px(本文幅 640px から枠 2px と内側の余白 48px を引いた値)に収まる、切りのよい一番大きい値。**値の正本は [figure/render.mjs](figure/render.mjs) の `SPEC` で、この表はそれを写したもの。**

### ラベルの上限 — 字幅で数える

**箱は固定幅で、和文の幅をその場では測らない。**代わりに Zen Kaku Gothic New 400 の送り幅を Chrome の `canvas.measureText` で一度だけ実測し、1/1000em で render.mjs に凍らせてある(0x20〜0x7e の 95 文字)。表に無い文字は全角として数え、半角カナだけ実測の 0.5 を使う。

**1 字幅 = 全角1文字ぶんの送り = 1.03em = 14.42px**(本文の字間 0.03em を含む)。上限 10 字幅は 144.2px で、箱の内寸 152px に収まる。六角形の 8 字幅は 115.4px で、尖りを引いた内寸 124px に収まる。

**半角を一律 0.5 と数える手は使えない。**実測では `W` が 0.844em、`m` が 0.799em ある。半角20文字は 10 字幅に見えて実際は 244.7px あり、箱から 100px はみ出す。

**ラベルと caption は1行。**改行と制御文字は `text/control` で落とす。箱は1行ぶんの高さしか無いので、入れても版が崩れるだけになる。分けたいことは figcaption の文へ回す。

### 使い方

```
node figure/deliver.mjs figure/examples/triage.json
node figure/deliver.mjs 入力.json --as page --out 図.html
```

`--as snippet`(既定)は `<figure class="gad-figure">` ごと標準出力へ出す。本文にそのまま置ける。`--as page` は tokens.css と document.css を埋め込んだ、1ファイルで開ける HTML。

**検査を通らなければ何も書かない。**診断を出して 1 で終わる。**終了コードが 0 でないものを「出せた」と呼ばない。**使い方が違うときと、入力を読めない・書き出せないときは 2。この3つを混ぜないので、呼ぶ側は番号だけで理由を分けられる。

診断は規則id・場所・数値・直し方の4つを必ず持つ。

```
[label/width] /flow/0/label 「設計から作り直して合意を取る」 14.0 字幅(201.9px)。branch(六角形)の上限は 8 字幅(115.4px)
    直し方: あと 6.0 字幅(86.5px)短くする。8 字幅なら「設計から作り直し」まで
```

入力の形の正本は [figure/schema.json](figure/schema.json)。`caption` は省けない。**図が意味を持つなら関係を文で書く、という規則を構造で守るために必須にしてある。**

**通った出力は凍結する。**生成物を手で直さない。直したいことがあれば入力の JSON を直して作り直す。出力の先頭にはその旨の1行が入る。

### 扱えない図

シーケンス、状態遷移、ER、ネットワーク、マインドマップ、ガント、データのグラフはこの生成器の外。**「図にしない」か、archify を使う。**型をそろえに行かない。1つで足りているうちは足さない。

## skin の差し替え

色と書体は tokens.css にしかない。**そこが G.A Design そのもの。**顧客ごとの skin は [profiles/](profiles/) に `<顧客名>.css` として置き、埋め込むときに tokens.css の代わりに使う。既定の skin は tokens.css そのものなので、profiles/ に複製は置かない。

document.css は skin を問わず共通で、色も寸法も持たない。skin を作ったら、墨4段と `--gad-sign` / `--gad-alert` を下の手順で測り直す。

## 測り方

1. 出力を Chrome で開く
2. 文字と背後の地、線として置いた色を取る。色は OKLCH の文字列で返ることがあるので、canvas に 1px 塗って `getImageData` で sRGB に直す
3. WCAG の相対輝度の式で比を出す
4. しきい値と照らす。文字と地は 4.5 以上(24px 以上、または太字で 19px 以上なら 3.0 以上)、線と印は 3.0 以上

**`getComputedStyle` が返す文字列を数値として読むと嘘の値が出る。**必ず canvas に塗ってから読む。

## 変えるとき

- 値は tokens.css だけを直す。document.css と成果物に色・寸法を直接書かない
- 墨4段か有彩色2つを触ったら、上の表の明暗差を測り直し、同じコミットで書き換える
- 値がこの文書と tokens.css で食い違ったら、tokens.css が正しい。成果物が読むのはそちらなので、この文書のほうを直す
- 根拠が書かれていない値を見つけたら、それはこの文書の欠落。測った数字か、決めた日と決めた人を足す
- 直したら `./check.sh` を回す。この文書の主張(墨の表の明暗差、有彩色2つ、しきい値、部品12種、値が token の外に出ていないこと)を機械で確かめて、崩れていれば 1 で終わる
